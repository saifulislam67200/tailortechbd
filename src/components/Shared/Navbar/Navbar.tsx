import TopBar from "./TopBar";

const Navbar = () => {
  return (
    <header>
      <TopBar />
      <nav>
        <h2 className="text-sm font-bold">Navbar</h2>
      </nav>
    </header>
  );
};

export default Navbar;
